<?xml version="1.0" ?>
<xs:schema
    xmlns:xs="http://www.w3.org/2001/XMLSchema"
    targetNamespace="http://nuxeo.org/schemas/indexation_rubrique_solon_epg/"
    xmlns:indexrub="http://nuxeo.org/schemas/indexation_rubrique_solon_epg/"
>

	<!-- on inclut le schema contenant la definition de xs:stringList -->
	<xs:include schemaLocation="base.xsd" />

	<!-- intitule de la rubrique -->
	<xs:element name="intitule" type="xs:string" />

</xs:schema>
