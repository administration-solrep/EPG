<?xml version="1.0" ?>
<xs:schema
    targetNamespace="http://www.nuxeo.org/ecm/schemas/elementFondDossierSolonEpg"
    xmlns:xs="http://www.w3.org/2001/XMLSchema"
    xmlns:fddf="http://www.nuxeo.org/ecm/schemas/elementFondDossierSolonEpg"
>

	<!-- on inclut le schema contenant la definition de xs:stringList -->
	<xs:include schemaLocation="base.xsd" />

	<!-- proprietes du repertoire fond de dossier -->

	<!-- info pour savoir si le répertoire est supprimable ou pas -->
	<xs:element name="isSupprimable" type="xs:boolean" default="true" />

</xs:schema>
