<?xml version="1.0"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
	targetNamespace="http://nuxeo.org/schemas/dossiers_signales/" xmlns:ds="http://nuxeo.org/schemas/dossiers_signales/">

	<!-- on inclut le schema contenant la definition de xs:stringList -->
	<xs:include schemaLocation="base.xsd" />

	<!-- ids dossiers -->
	<xs:element name="dossiersIds" type="ds:stringList" />

</xs:schema>